<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chen.zucc.com.personalassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Schedule.ScheduleActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Income_expenses.Income_expensesActivity" />
        <activity android:name=".Manage_money_matters.Manager_money_mattersActivity" />
        <activity android:name=".Personal_homepage.homepage" />
        <activity
            android:name=".Personal_homepage.SettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Personal_homepage.AccountsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Personal_homepage.PersonalSettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Personal_homepage.RetroactionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>